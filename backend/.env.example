# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=
APP_SHARE_DIR=var/share
###< symfony/framework-bundle ###

###> symfony/routing ###
# Configure how to generate URLs in non-HTTP contexts, such as CLI commands.
# See https://symfony.com/doc/current/routing.html#generating-urls-in-commands
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> doctrine/doctrine-bundle ###
# Format described at https://www.doctrine-project.org/projects/doctrine-dbal/en/latest/reference/configuration.html#connecting-using-a-url
# IMPORTANT: You MUST configure your server version, either here or in config/packages/doctrine.yaml
#
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"
# DATABASE_URL="mysql://app:!ChangeMe!@127.0.0.1:3306/app?serverVersion=8.0.32&charset=utf8mb4"
DATABASE_URL="mysql://app:!ChangeMe!@127.0.0.1:3306/app?serverVersion=11.4.12&charset=utf8mb4"
###< doctrine/doctrine-bundle ###

###> nelmio/cors-bundle ###
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'
###< nelmio/cors-bundle ###

###> app/ai_providers ###
# Options: 'ollama' or 'api_endpoint'
AI_PROVIDER=ollama

OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_EMBEDDING_MODEL=mxbai-embed-large
OLLAMA_ANALYSIS_MODEL=gpt-oss:20b
OLLAMA_CHAT_MODEL=gpt-oss:20b

# OpenAI-compatible API endpoint (used when AI_PROVIDER=api_endpoint, or as a
# fallback when no active AiProviderConfig row exists for a usage)
AI_API_ENDPOINT=https://oai.endpoints.kepler.ai.cloud.ovh.net/doc/gpt-oss-120b/v1/chat/completions
AI_API_KEY=
AI_API_MODEL=gpt-oss-120b
AI_API_TIMEOUT=30
###< app/ai_providers ###

###> app/vector_connector ###
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_API_KEY=
###< app/vector_connector ###

###> app/security ###
# Multi-user (backoffice + API): accounts live in the app_user table, create
# them with:
#   docker exec chatbot-symfony php bin/console app:user:create <email> <password>
# These two vars just seed the first app_user row on initial migration (and
# ADMIN_USERNAME/ADMIN_PASSWORD_HASH aren't read by Symfony afterwards).
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=
# Plaintext counterpart of the hash above -- used only by the Nuxt frontend
# proxy (compose.yaml, service "nuxt") to authenticate via HTTP Basic on
# behalf of the public chat widget. Must match a real app_user account.
ADMIN_PASSWORD=
###< app/security ###

###> symfony/messenger ###
# "redis" = the compose service name (backend/compose.yaml), reachable from
# app/worker containers -- not localhost.
MESSENGER_TRANSPORT_DSN=redis://redis:6379/messages
###< symfony/messenger ###

###> app/cache ###
# Same Redis server as MESSENGER_TRANSPORT_DSN above, but a plain DSN (no
# stream name) -- config/packages/cache.yaml's default_redis_provider needs
# a connection string, not a messenger-specific transport DSN.
REDIS_URL=redis://redis:6379
###< app/cache ###

###> symfony/mailer ###
# null://null = no-op transport, nothing is actually sent (safe default).
# Docker dev overrides this to smtp://mailer:1025 (backend/compose.yaml) --
# a local MailHog catcher, view caught mail at http://mailhog.chatbot.localhost.
# In prod, point this at a real transactional-email provider/SMTP relay.
MAILER_DSN=null://null
###< symfony/mailer ###

###> app/workflows ###
# "From" address used by the email step type (App\Workflow\WorkflowExecutionService).
# Must be a domain/address your MAILER_DSN provider is allowed to send as.
MAILER_FROM_ADDRESS=noreply@chatbot.localhost

# Cal.eu API key for the "planifier_entretien" workflow's "Reserver sur
# Cal.eu" api_call step -- referenced from that step's `configuration.headers`
# as "%env(CAL_EU_API_KEY)%" (WorkflowExecutionService::resolveEnvHeaders()),
# never stored in plaintext in the workflow_step row.
CAL_EU_API_KEY=
###< app/workflows ###

###> app/chat ###
# Where ChatService pings the moment a visitor starts a new conversation.
# Leave empty to disable the notification.
OWNER_NOTIFICATION_EMAIL=
# Conversations (and their messages) with no activity for longer than this
# are deleted by `bin/console app:conversations:purge` -- see
# PurgeConversationsCommand and docs/DEPLOYMENT.md for the cron setup.
CONVERSATION_RETENTION_DAYS=90
###< app/chat ###
